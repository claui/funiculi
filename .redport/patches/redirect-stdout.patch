diff --git a/hooks/post_gen_project.py b/hooks/post_gen_project.py
index 3437158..b2486d3 100644
--- a/hooks/post_gen_project.py
+++ b/hooks/post_gen_project.py
@@ -17,13 +17,14 @@ poetry_environment.update({
     'PYTHON_KEYRING_BACKEND': 'keyring.backends.null.Keyring',
 })
 
-print('Running poetry. This may take a while.')
+print('Running poetry. This may take a while.', file=sys.stderr)
 try:
     subprocess.run(
         'poetry install',
         env=poetry_environment,
         check=True,
         shell=True,
+        stdout=sys.stderr,
     )
 except subprocess.CalledProcessError as e:
     print(
@@ -31,6 +32,7 @@ except subprocess.CalledProcessError as e:
         'Fix any issues, then go to the '
         '{{ cookiecutter.project_slug }} directory and re-run:',
         f'    {e.cmd}',
+        file=sys.stderr,
         sep='\n',
     )
 {%- endif %}
